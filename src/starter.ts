// @TODO remove this file once there is a file with some code in it.
const a = 'this file just makes ts happy'
const b = 'though delte this once there is'
const c = 'another .ts or .ts* file'

export function blah() {
  return a + b + c
}
